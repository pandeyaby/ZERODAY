# Legacy path — use examples/factory/ instead
#
# Offensive kill-chain / mission theater was removed from the product surface.
# Defensive desk examples live under examples/factory/*.yaml

see: ../factory/crowdstrike-aws-desk.yaml
