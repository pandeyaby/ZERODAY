# Examples — org ops package

| File | Purpose |
|------|---------|
| [zeroday-org-locate.yml](./zeroday-org-locate.yml) | Copy-paste GitHub workflow for private org clone → keyless locate → SARIF / PR comment |

See [`docs/org-ops-runbook.md`](../../docs/org-ops-runbook.md) and
[`docs/cassette-runbook.md`](../../docs/cassette-runbook.md).

**Defaults:** keyless (`rules` or `recording`). Live Antares is never in CI.
**Never** auto-merge. Localization ≠ exploitability.
