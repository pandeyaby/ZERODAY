# First-time users

1. `npm install`
2. `npm run zeroday -- operate --cwe CWE-89 --fixture`
3. `npm run zeroday -- verify --from <printed-run-dir>`
4. Optional: `npm run play` → http://localhost:3333/play

You do not need a GPU or Hugging Face token for the fixture / keyless path.
